#{

N := 10;
acc := 0;

for i in range(1, N) loop
  acc := acc + i;
end loop;

success(acc = (N*(N+1)) / 2);

M := 12;
i := 1;
acc := 0;
while i < M loop
  acc := acc + i;
  i := i+1;
end loop;

success(acc = (M*(M-1))/2);

return true;
}#


